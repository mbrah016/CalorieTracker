[build]
  command = "npx expo export --platform web && node inject-key.js $GEMINI_API_KEY"
  publish = "dist"

[build.environment]
  NODE_OPTIONS = "node:worker_threads"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
